{#
/**
 * @file neo-icon.html.twig
 * Default theme implementation to present a single Neo icon.
 *
 * @see template_preprocess_neo_icon()
 *
 * @ingroup themeable
 */
#}
{% if icon %}<{{ tag }}{{ attributes }}>{{ children }}</{{ tag }}>{% endif %}
